import { PreviewCartClient } from "@/components/preview/PreviewCartClient";

export const metadata = {
  title: "السلة (معاينة)",
};

export default function PreviewCartPage() {
  return <PreviewCartClient />;
}
