/**
 * المنتجات الأكثر طلباً — بالقياس، لا بالتخمين.
 *
 * لماذا هذه القائمة موجودة أصلاً: الصفحة الرئيسية كانت تعرض أول 16 منتجاً
 * بترتيب الكتالوج الافتراضي (`sort_order` ثم `created_at`)، وهو ترتيب إداري
 * لا علاقة له بما يطلبه الزبناء. النتيجة أن أول ما يراه الزائر كان
 * «تلكموند مكيف» و«بولي LG»، بينما لم يكن أيٌّ من المنتجات الخمسة الأكثر
 * طلباً بينها إطلاقاً.
 *
 * الأرقام التي بُنيت عليها (25–31 أغسطس 2026، `analytics_events`):
 *
 *   TF-RF-R134-2-5KG   80 مشاهدة · 15 إضافة للسلة
 *   TF-RF-R134-900g    73 مشاهدة · 32 إضافة  ← الأعلى إضافةً للسلة
 *   TF-RF-R22-3KG      63 مشاهدة
 *   TF-AC-019          53 مشاهدة · 19 إضافة
 *   TF-RF-R22-900g     52 مشاهدة · 13 إضافة
 *   TF-AC-017          23 مشاهدة
 *   TF-GWH-001         20 مشاهدة
 *   TF-RF-076          18 إضافة
 *   TF-RF-BOSCH-001    18 إضافة
 *   TF-SAW-001         17 إضافة
 *   TF-RF-065          15 إضافة
 *   TF-RF-067          15 إضافة
 *
 * الغازات وحدها جمعت 268 مشاهدة من أصل 523 لكل المنتجات — أي أكثر من نصف
 * كل اهتمام الزبناء بالكتالوج كله.
 *
 * الترتيب هنا مقصود: الطلب المقاس أولاً، والإضافة للسلة تُرجّح على المشاهدة
 * وحدها لأنها نيّة شراء أوضح.
 *
 * هذه قائمة تُراجَع يدوياً عن قصد، لا استعلام حيّ على جدول القياس: الصفحة
 * الرئيسية تُصيَّر عند كل طلب، وإضافة استعلام تجميعي على `analytics_events`
 * إليها تُثقلها لأجل ترتيب لا يتغيّر إلا كل بضعة أسابيع. تُحدَّث بإعادة قياس
 * نفس الاستعلام حين تتغيّر صورة الطلب.
 */
export const TOP_DEMAND_SKUS = [
  "TF-RF-R134-900g",
  "TF-RF-R134-2-5KG",
  "TF-RF-R22-900g",
  "TF-RF-R22-3KG",
  "TF-AC-019",
  "TF-RF-076",
  "TF-RF-BOSCH-001",
  "TF-SAW-001",
] as const;

/** كم منتجاً يُعرض في قسم «الأكثر طلباً». */
export const TOP_DEMAND_LIMIT = 8;
