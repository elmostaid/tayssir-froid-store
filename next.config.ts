import type { NextConfig } from "next";

// next/image يرفض عرض أي صورة من مضيف خارجي غير مُدرَج صراحةً فـ
// remotePatterns (يرمي استثناءً وقت التصيير — بالضبط سبب "Server Error"
// المُبلَّغ عنه فـ/admin/products بعد ظهور أول صورة من Supabase Storage
// الحقيقي). نشتق النطاق تلقائياً من NEXT_PUBLIC_SUPABASE_URL نفسه (نفس
// المتغيّر الذي يبني عليه resolveProductImageUrl الرابط العام) بدل تكراره
// يدوياً؛ فارغ محلياً حيث لا يوجد هذا المتغيّر، فلا يتغيّر شيء فالتطوير
// المحلي (الصور المحلية لا تحتاج remotePatterns أصلاً).
function supabaseStorageRemotePattern(): NonNullable<NextConfig["images"]>["remotePatterns"] {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
  if (!url) return [];
  try {
    const { hostname } = new URL(url);
    return [
      {
        protocol: "https",
        hostname,
        pathname: "/storage/v1/object/public/**",
      },
    ];
  } catch {
    return [];
  }
}

const nextConfig: NextConfig = {
  // مسارات توليد PDF (بون التحضير ووصل الزبون) تقرأ ملفات الخط العربي محلياً
  // عبر path.join(process.cwd(), ...) وقت التشغيل بدل import ثابت، وقد لا
  // يكتشفها تتبّع الملفات التلقائي لـVercel دائماً — نضمن تضمينها صراحةً.
  outputFileTracingIncludes: {
    "/admin/orders/[id]/picking-slip.pdf": [
      "./src/lib/pdf/fonts/**/*",
      "./public/brand/**/*",
    ],
    "/order/[reference]/receipt.pdf": [
      "./src/lib/pdf/fonts/**/*",
      "./public/brand/**/*",
    ],
  },
  images: {
    // صور المنتجات القديمة محلية داخل public/product-images وتمر عبر مُحسِّن
    // next/image بدون أي remotePatterns لأنها مسارات محلية. الصور المرفوعة
    // حديثاً إلى bucket "product-images" الحقيقي فـSupabase Storage تحتاج
    // مضيفها مُدرَجاً هنا — يُضاف تلقائياً أعلاه.
    remotePatterns: supabaseStorageRemotePattern(),
    // ⚠️ تاريخ هذا الإعداد (مهم قبل تغييره مجدداً):
    // unoptimized: true جُرِّب سابقاً (commit 3099b0b) لحل صور مكسورة متقطّعة
    // على بعض الهواتف — أثبت التشخيص اللاحق (فيديو حقيقي + فحص شامل) أن
    // السبب الفعلي لم يكن صيغة الملفات (كلها سليمة)، بل تذبذب محسِّن
    // /_next/image نفسه تحت الحمل (فورة طلبات تحويل لعدة صور فآنٍ واحد،
    // خصوصاً عند تبديل الجهاز لوضعية Desktop التي تُضاعف عدد الصور بالشبكة).
    // unoptimized: true أوقف هذا التذبذب فعلاً، لكن كشف مشكلة أخطر: كل صور
    // دفعة product-images-v3 (والمصادر الأقدم) كانت PNG أصلية ضخمة (متوسط
    // ~1.5 ميغابايت، حتى 3+ ميغابايت للملف الواحد) — بدون تصغير/ضغط الخادم
    // التلقائي، كل صفحة تصنيف (حتى 77+ منتج) أصبحت تنقل عشرات الميغابايتات
    // فور فتحها، ما تسبَّب فانقطاعات اتصال (ERR_CONNECTION_ABORTED) حقيقية
    // لزبناء بتغطية إنترنت ضعيفة — أثر أوسع وأخطر من العطل الأصلي.
    // أُعيد unoptimized إلى الوضع الافتراضي (محذوف = false) هنا كإجراء طارئ
    // فوري لإرجاع خفّة الموقع. الحل الحقيقي طويل المدى (غير منفَّذ هنا بعد):
    // migration جاهزة أصلاً (supabase/migrations/20260815000000_*.sql) تُحوِّل
    // storage_path لنسخ JPEG مضغوطة مسبقاً (متوسط ~170 كيلوبايت، أُنشئت في
    // نفس هذا الالتزام) بدل الأصول الثقيلة — بعدها سيعمل محسِّن /_next/image
    // على ملفات صغيرة أصلاً بدل PNG ضخمة، فيقلّ احتمال تذبذبه تحت الحمل
    // بشكل كبير مع بقاء الصفحة خفيفة. يجب تطبيق تلك migration على قاعدة
    // الإنتاج (عبر supabase db push) لإغلاق هذا الملف نهائياً.
    //
    // minimumCacheTTL: جُرِّبت قيمة سنة كاملة (31536000) في نفس الالتزام
    // أعلاه — أُعيدت هنا لساعة واحدة فقط (3600) كإجراء احترازي بعد بلاغ
    // صور تصنيفات مكسورة بالجملة على الصفحة الرئيسية بعد ذلك النشر مباشرة.
    // بناء محلي طازج (بلا أي cache سابق) من نفس الالتزام الذي رفع القيمة
    // لسنة لم يُظهر أي فشل فعلي فـ12/12 صورة تصنيف (200 لكل واحدة) — يعني
    // لم يثبت وجود عطل حقيقي فالكود/الملفات نفسها بهذا الفحص. لكن بما أنني
    // لا أملك وصولاً لفحص أو تفريغ ذاكرة Vercel المؤقتة الفعلية لمحسِّن
    // الصور، ساعة واحدة تحدّ أقصى مدة ممكنة لبقاء أي استجابة فاشلة (لو
    // حدثت فعلاً أثناء انتقال النشر) محفوظة، بدل سنة كاملة — بلا التخلي عن
    // فائدة التخزين المؤقت كلياً (كانت 60 ثانية فقط قبل هذا الملف أصلاً).
    minimumCacheTTL: 3600,
  },
};

export default nextConfig;
